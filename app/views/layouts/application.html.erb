<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <!-- MME el js es necessari per a que funcionin les confirmacions al esborrar per exemple -->
    <%= javascript_include_tag "application" %>
    <!-- MME es l'icon que surt a la barra de direccio -->
    <%= auto_discovery_link_tag :rss %>
    <%= auto_discovery_link_tag :atom %>
    <%= csrf_meta_tag %>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
    <div class="container">
      <%= render 'layouts/header' %>
      
      <section class="round">
        <% if flash.any? %>
          <% flash.each do |key, value| %>
            <div class="flash <%= key %>"><%= value %></div>
          <% end %>
        <% end %>
        <%= yield %>
      </section>

      <%= render 'layouts/footer' %>
      <% if Rails.env.development? %>
        <%= render 'layouts/debug_info' %>
      <% end %>
    </div>  
    <!-- Exemple de script: llegeix les cookies -->
    <p>
    <strong> Mostra les teves cookies </strong>
    <script>document.write(document.cookie);</script>
    </p>
  </body>
</html>