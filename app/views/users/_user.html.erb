<li>
  <%= gravatar_for user, :size => 25 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
     | 
    <%= link_to 'Delete', user, 
        method: :delete,
        confirm: 'Are you sure?', 
        title: "Delete #{user.name}"  # tootltip
    %>
  <% end %>
</li>
